# ── Dependencies ─────────────────────────────────────────
node_modules/
api/node_modules/
admin/node_modules/

# ── Secrets — NEVER push ─────────────────────────────────
api/.env
.env

# ── OS files ─────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Logs ─────────────────────────────────────────────────
*.log
npm-debug.log*

# ── Local uploads (Cloudinary handles production) ────────
api/uploads/*
!api/uploads/.gitkeep

# ── Keep admin/dist so Vercel can serve it ───────────────
!admin/dist/
